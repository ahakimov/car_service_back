spring.application.name=car-repair-shop
spring.profiles.active=h2

# CORS Configuration - Use * to allow all origins (for deployed frontend)
# Or specify specific origins like: http://localhost:3000,https://yourdomain.com
app.cors.allowed-origins=*

server.port=9341